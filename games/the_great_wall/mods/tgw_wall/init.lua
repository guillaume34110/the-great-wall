-- tgw_wall : Pre-generated 150-block wall, HP per node, shared-wallet repair.
-- API: tgw_wall.build(), repair(pos), damage(pos,amount)

local MP = core.get_modpath(core.get_current_modname())
local S = core.get_translator("tgw_wall")

tgw_wall = {}
tgw_wall.S = S

core.log("action", "[tgw_wall] loaded")

-- TODO: implement
