-- tgw_capture : Non-lethal weapons (net, taser, cuffs). Hand off to pipeline.
-- API: tgw_capture.register_tool(name,def), capture(invader)

local MP = core.get_modpath(core.get_current_modname())
local S = core.get_translator("tgw_capture")

tgw_capture = {}
tgw_capture.S = S

core.log("action", "[tgw_capture] loaded")

-- TODO: implement
